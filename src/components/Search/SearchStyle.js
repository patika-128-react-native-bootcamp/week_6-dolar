import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    backgroundColor: '#e0e0e0',
    padding: 5,
    borderRadius: 5,
    borderWidth: 1,
    borderColor: '#bdbdbd',
  },
});
